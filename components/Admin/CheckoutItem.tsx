import React from 'react';

const CheckoutItem = () => {
    return (
        <div>
            <p>checkout item</p>
        </div>
    );
};

CheckoutItem.defaultProps = {};

export default React.memo(CheckoutItem);
